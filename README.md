# InteractiveWebsite (DocAgent landing) — Local Setup

## Prereqs
- Node.js 18+ (recommended 20+)

## Run locally
```bash
cd web
npm install
npm run dev
```

Open http://localhost:3000

## Notes
- Gradient background is in `web/src/app/layout.tsx`
- Main page is `web/src/app/page.tsx`
